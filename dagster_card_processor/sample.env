GOOGLE_API_KEY=
MODEL_NAME=models/gemini-2.5-flash-preview-05-20

# --------------------------------------------------
# NEW: Dagster Asset Configuration
# These values will now be the defaults for your pipeline.
# You can change them here without touching the Python code.
# --------------------------------------------------
INPUT_DIR="cards_to_process"
OUTPUT_DIR="output"
PDF_BASE_URL="http://localhost:8000"

# The prefix used in DBT descriptions to identify system-managed fields.
SYSTEM_INJECTED_PREFIX="[SYSTEM-INJECTED]"