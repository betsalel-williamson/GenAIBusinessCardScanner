version: 2

sources:
  - name: raw_card_data
    description: "Raw JSON files generated by the Gemini extraction pipeline."
    tables:
      - name: validated_json_blobs
        description: "Represents a single validated JSON file containing a list of card objects."
